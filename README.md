# ProjectBuddy — LangChain ReAct Agent (Excel-backed)

## Setup
1. Create a virtual environment and install:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
